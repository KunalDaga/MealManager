import UIKit

class WelcomeViewController: UIViewController {
    override func viewDidLoad() {
        super.viewDidLoad()
    }
    
    @IBAction func addMealPressed(_ sender: UIButton) {
        //segue set up
    }
    
    @IBAction func viewMealsPressed(_ sender: UIButton) {
        //segue already set up
    }
    
    @IBAction func randomiseMealPressed(_ sender: UIButton) {
        //already segue set up to take to RandomMealViewController
    }
}
